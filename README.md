# marchfra's dotfiles

These are my dotfiles. To install them, do

```shell
git clone https://github.com/marchfra/dotfiles.git ~/dotfiles
cd ~/dotfiles
```

then run

```shell
stow [program]
```

for each program you want to copy the configuration of.
